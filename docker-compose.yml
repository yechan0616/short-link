services:
  web:
    # 이미지는 환경변수 IMAGE로 주입 (예: ghcr.io/OWNER/short-link:latest)
    build: .
    container_name: short-link-web
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    # 필요 시 앱 환경변수 파일(선택)